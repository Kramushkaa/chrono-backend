meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  steps:
    - npm ci
    - npm run build
  artifacts:
    dist: /dist
run:
  scriptName: dist/server.js
  persistenceMount: /data
  containerPort: 3001
